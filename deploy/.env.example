# Copy to deploy/.env and fill in. Compose reads this automatically.

# The public hostname Caddy serves (DNS A/AAAA record must point at this box
# before first start, or Let's Encrypt issuance fails).
FOGHORN_DOMAIN=foghorn.example.com
